# Runtime-база сервера (создаётся автоматически при запуске node server/index.js)
server/data/db.json
server/data/db.json.tmp

# Node / всякое
node_modules/
*.log
.DS_Store

# VIP-коды в открытом виде — только у владельца, в репозиторий НЕ попадают (в клиенте лишь хеши)
vip_codes_funpay.txt
