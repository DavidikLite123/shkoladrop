# 🎒 ШКОЛА ДРОП — браузерный симулятор школьных кейсов и честного апгрейдера

**Сезон 4.1 «Тихая безопасность»**: панель владельца переехала на сервер (в браузере больше нет кодов и секретов — только токен на 12 часов), сайт и сервер отдают только саму игру, утёкшие VIP-коды и коды админки аннулированы, а база данных уезжает во внешний «сейф» и переживает сон бесплатного Render. Подробности — в [`docs/БЕЗОПАСНОСТЬ_И_ДАННЫЕ.md`](docs/БЕЗОПАСНОСТЬ_И_ДАННЫЕ.md).

**Сезон 3.7 «Большой вайп»**: e-mail аккаунты с входом по коду, глобальный вайп экономики (ник и уникальный ID — на месте!), жёсткие кейсы, лавка из 4 базовых вещиц и **10 предметов, которые добываются ТОЛЬКО в апгрейдере**.

Официальный проект **David Lite / David Lite Studio**. Игра работает прямо в браузере (десктоп и мобильные), не требует установки, а онлайн-плюшки (чат, ID, галочки, облачные сейвы, аккаунты, подарки, трейдинг, спонсорство) живут на зашитом сервере сообщества — играть можно и оффлайн.

🌐 **Игра:** https://shkoladrop.vercel.app/
🖥 **Сервер сообщества (зашит):** https://shkoladrop.onrender.com
▶ **Автор:** https://www.youtube.com/@DavidliteYT

---

## 📖 Что это вообще такое

Шуточный «слот-симулятор» про школу: открываешь кейсы (школьные, CS2, игровые, кошачьи, ютубер-кейсы и бета-лаборатория), выловленный лут продаёшь или **заносишь в честном апгрейдере** (ставишь свой предмет против цели дороже — шанс победы считается честно по ценам). Весь рандом — на устройстве игрока через `crypto.getRandomValues`, таблица «Шансы» показывает настоящие цифры.

Кроме кручения кейсов в игре есть: уровни и титулы, 27 достижений, ежедневные награды со стриком, пассивное «Дежурство» (оффлайн-доход), промокоды и VIP-коды, 165 кейсов, крафт и коллекции, секретный кейс с Котом-Хранителем за 10 000 000 ₽ и куча школьного юмора. **Реальных денег в игре нет — это фан-симулятор.**

---

## 🎮 Мини-игры: единое меню и 🚀 Ракета

### 1. Нижнее меню из 4 кнопок + разделы внутри экранов

Раньше в нижнем меню было 7 кнопок, и подписи в них не помещались. Теперь там
**4 крупные кнопки** — **🎮 Игры**, **🎒 Рюкзак**, **💬 Сообщество**, **🏪 Лавка**,
а родственные экраны живут внутри своей кнопки:

- **🎮 Игры** — меню карточек: **📦 Кейсы**, **⚡ Апгрейд**, **🚀 Ракета** (NEW),
  **🍾 Бутылочка** (NEW) и **🎮 Режимы** (хардкор/песочница — раньше была отдельной кнопкой);
- **🎒 Рюкзак** — переключатель сверху: **Рюкзак · 🔨 Крафт · 📚 Коллекции**
  (из рюкзака в крафт и обратно — одной кнопкой, нижнее меню подсвечивает раздел целиком).

Индекс текущего экрана по-прежнему `switchTab('craft')` и т.п. — переключатель
раздела рисует `renderItemsSegNav()` в `js/game.js` (кнопка нижнего меню для крафта,
коллекций и режимов вычисляется в `navButtonForTab`).

Меню живёт на конфиге: список игр — массив `MINI_GAMES` в `js/config.js`
(рендер и выбор — `js/games.js`). Чтобы добавить новую игру, достаточно одной записи:

```js
MiniGames.register({
  id: 'dice', tab: 'dice', icon: '🎲', name: 'Кости',
  desc: 'Угадай сумму и забери выигрыш',
  enabled: () => true          // false — карточка затемнена
});
```

плюс панель `<main id="viewDice" class="view-panel ... hidden">` в `index.html` и ветка в
`switchTab()` — меню подхватит игру само, без правок навигации.

### 2. 🚀 Ракета (Crash)

Классическая краш-механика в школьных декорациях:

| Что | Как работает |
|-----|--------------|
| Поле полёта | канвас: логарифмический график набора высоты, сетка высот, крупный множитель. Сама **ракета — отдельный SVG-спрайт** (`#crashRocket`, заливной вектор из `js/icons.js`) поверх канваса: чёткая на любом DPR, с пламенем и наклоном по траектории, никаких эмодзи |
| Ставка | лимиты: минимум **100 ₽**, максимум **50 000 000 ₽** и не больше текущего баланса; быстрые кнопки ½, ×2, MAX |
| Списание | ставка списывается с баланса **мгновенно, в момент нажатия «Запустить»**; проверка идёт трижды (лимиты → баланс → предохранитель перед списанием), поэтому отрицательный баланс невозможен |
| Автовывод | необязательное поле: ракета заберёт выигрыш сама ровно на указанном `X` (от 1,01×) |
| «Забрать» | активна только в полёте, на кнопке — текущая сумма `ставка × текущий X` |
| Блокировки | на время полёта поля ставки/автовывода и «Старт» отключены, повторный старт игнорируется — двойное списание невозможно |

**Математика (честная, считается на устройстве игрока):**

- Точка краша определяется **до старта** полёта через `crypto.getRandomValues`
  (общий `RNG.float()` проекта) — подкрутить по ходу нельзя.
- Распределение — **«коридор + хвост»** (щедрый профиль):
  `P(долететь до X) = (1 − earlyChance) · (earlyEdge / X)^tailAlpha`.

  | X | 1,5× | 2× | 3× | 4× | 5× | 10× | 25× | 50× |
  |---|------|----|----|----|----|-----|-----|-----|
  | P(долететь) | 92% | 70% | 45% | 32% | 25% | 12% | 4% | 1,9% |

- **Пол `minCrash = 1,20×`:** раньше этого множителя ракета не взрывается **никогда**
  (по старой формуле 5% раундов сгорали мгновенно на 1,00×). Ранний взрыв — это
  всего ~10% раундов в коридоре 1,20–1,60×, и даже он наступает не раньше 2,4 с
  полёта: игрок стабильно успевает среагировать и нажать «Забрать».
- Средняя точка краша ≈ 2,7× (было 1,9×). Максимальный возврат **RTP ≈ 145%**
  при выводе на 1,6–1,8×, на 10× — 116%, на 50× — 95%. Это осознанный выбор:
  игра должна радовать выигрышами. Вернуть «честные» 95% можно одной правкой
  конфига — см. комментарий к `CRASH_CONFIG` в `js/config.js`.
- **Фаза разгона `takeoffSec = 1,2 с`:** пока она идёт, множитель держится ровно
  `1,00×`, ракета дрожит на площадке с работающим двигателем, взрыв невозможен.
- **Кривая полёта:** `X(t) = e^(k1·τ + k2·τ²)`, где `τ = t − takeoffSec`,
  `k1 = 0,14`, `k2 = 0,012`. В начале рост почти линейный и предсказуемый
  (за первую секунду множитель прибавляет ~0,16), дальше разгоняется —
  азарт на высоких коэффициентах сохранён (2,00× ≈ за 5 с, 10× ≈ за 10,4 с).
- Горизонталь графика — фиксированное окно `windowSec = 9 с`: ракета **едет
  слева направо по полю**, а когда полёт длиннее окна — график прокручивается.
  Шкала высот логарифмическая и сама расширяется (50× → 100× → …), поэтому
  ракета не «прилипает» к потолку на больших коэффициентах.
- Формулы вынесены в `js/config.js` чистыми функциями (`crashPointFromRoll`,
  `crashReachChance`, `crashRtpAt`, `crashMultiplierAt`, `crashTimeToMultiplier`,
  `crashPayout`) — их же проверяют автотесты.

**Обратная связь:** при взрыве — красная рамка поля, тряска, частицы, плашка «ВЗРЫВ!»
(векторная иконка) и низкий гул; при успешном выводе — зелёный акцент, конфетти
и восходящий звук кассы. Вся статистика (раунды, победы, лучший X, профит, лента
последних раундов) сохраняется в сейве, за ракету начисляются опыт и 4 достижения.

**Иконки без эмодзи.** Интерфейс мини-игр использует встроенные SVG-иконки
(`js/icons.js`, сетка 24×24, рисуются `currentColor`, без CDN и внешних шрифтов) —
на системах без эмодзи-шрифтов больше не бывает пустых квадратов. Ракета в поле
полёта — заливной SVG-спрайт (`rocketSolid`), а не символ эмодзи; если DOM-спрайт
почему-то недоступен, включается векторный фолбэк прямо внутри canvas.

**Адаптивность.** Ширина оболочки задана переменной `--app-max`
(28rem на телефоне → 34rem на планшете/ПК → 36rem на широких мониторах),
панель ракеты ограничена `max-width: 34rem` и отцентрована; поле полёта
резиновое (`clamp()` + `vh`), на ПК панель ставки становится двухколоночной,
поля ввода — 44 px, кнопки — 48 px (удобно под палец), горизонтальный скролл
заблокирован, есть отдельные правила для экранов ≤ 360 px.

## 🧨 Главное сезона 3.7

### 1. 🧹 Глобальный вайп — но ник и ID остаются
- При первом запуске 3.7 прогресс всех игроков обнуляется до стартового новичка: баланс, рюкзак, статистика. **Аккаунт выживает целиком: ник, уникальный ID (#123456), галочка верификации и привязанный e-mail.** Тот самый «все останется название id».
- Вайп выполняется дважды надёжно: **на клиенте** (сейв формата v11 и старше больше не поднимается — из него переносится только личность) и **на сервере** (одноразовая миграция: облачные сейвы/подарки/трейды чистятся, игроки и аккаунты не трогаются). Задумано для честного нового сезона.

### 2. 🔐 Шлагбаум «Введи аккаунт» с e-mail + паролем + кодом
- На входе без профиля открывается запертое окно **«Введи аккаунт»**: e-mail + пароль → 6-значный код «из письма» → вход. Сервер сам понимает, вход это или регистрация.
- **Демо-режим письма:** настоящего SMTP нет — код генерирует сервер и показывает прямо в игре (в жёлтой рамке). Механика входа/восстановления при этом настоящая.
- Возвращающийся игрок вводит ту же почту и пароль → получает код → **возвращается в свой аккаунт с любого устройства: тот же uid, тот же ID, облачный прогресс подтягивается сам.**
- Можно отказаться от e-mail и играть локально — но шлагбаум честно предупреждает: *«Если из такого аккаунта выйти — мы уже не виноваты»*. Привязать почту можно потом: ⚙️ Настройки → «🔒 Привязать e-mail».
- Пароли хранятся только солёным SHA-256 хешем, код живёт 10 минут и гаснет после 6 неверных попыток.

### 3. ⚡ Предметы ТОЛЬКО из апгрейдера
- Новая лестница из **10 апгрейд-эксклюзивов** (3 300 ₽ → 24 000 000 ₽): от «Пера завуча» до «Призрака вечного отличника».
- Их **нельзя купить в лавке и нельзя выбить из кейсов** — только занести в честном апгрейдере. Отдельный фильтр «⚡ Только апгрейд» в выборе цели и в рюкзаке.

### 4. 🥟 Лавка = 4 базовые вещицы
- Продаются только: холодная котлета в тесте (булочка!) 150 ₽, погрызанная ручка 300 ₽, ластик 500 ₽, банановая кожура 700 ₽.
- Всё остальное добывается через кейсы (жёстко!) и апгрейд (честно!).

### 5. 💥 Жёсткие кейсы
- Кейсы открываются всегда, сколько влезет — но «годнота» (предмет дороже кейса) проходит через **ярусные ворота удачи**: в 1.5× дороже — вес ÷2.9, в 6× — ÷8, в 25× — ÷20.
- Штрафы редкости ужесточены (covert 0.012 против старых 0.025, secret 0.0012 против 0.004), а 5 среднешкольных кейсов получили честных «лузеров» в пулах. Итог: EV всех кейсов ≤ ~1.0× цены, шанс годноты — единицы процентов, «налог миллионера» для богачей никуда не делся. Таблица «Шансы» всё это показывает честно.

### 6. 👋 Приветствие при входе
- Раз в 12 часов всплывает: «Привет, «ник»! Загляни в свой аккаунт — вдруг тебе уже выдали ✔ галочку верификации либо 🎁 код автора? Твой ID вот он…» — ID копируется нажатием в профиле и в «💬 Сообществе».
- Карточка «Сообщество» на вкладке «Сообщество» сразу показывает твой ID.

### 6½. 🧹 Дежурство: доход в секунду починен
- Раньше «Доход в секунду» на вкладке «Сообщество» всегда показывал **0 ₽/сек**, а накопление не шло: удалённая функция `renderPromoList()` роняла `initGame()` до запуска тикера. Функция возвращена, запуск больше не падает.
- Начисление теперь идёт **по реальному времени** (`idle.lastTick`), а не по тикам `setInterval`: в свёрнутой вкладке и на заблокированном телефоне браузер душит таймеры, из-за чего капал ~1% дохода. Теперь считается каждая секунда отсутствия — и при возврате в вкладку, и при перезаходе.
- Оффлайн-доход — **100% ставки** (было 40%) с потолком 12 часов; уведомление «Пока тебя не было…» показывает часы и минуты.
- Покупка нового уровня досчитывает старую ставку и мгновенно обновляет «₽/сек»; подсказка при наведении показывает доход в минуту и в час.

### 6¾. 🐹 Легендарный хомяк, коды и защита владельца
- **ЛЕГЕНДАРНЫЙ ХОМЯК ★ ВЕЧНАЯ ПАМЯТЬ** — мемориальный предмет за **1 000 000 000 000 000 000 000 ₽** (миллиард триллионов), самый дорогой в игре. Выпадает только из Кейса миллиардера с исчезающе малым шансом (`assets/legend-hamster.jpg`).
- **VIP-утечка закрыта дважды.** Раньше коды лежали открытым текстом в `js/config.js`, потом — списком в `codes/vip-codes.md`, который раздавали и сайт, и сервер. Теперь в клиенте только SHA-256-хеши, открытый список живёт в `vip_codes_funpay.txt` (в `.gitignore`), файлы `codes/` больше не публикуются, а **все ранее опубликованные коды аннулированы** (ротация: `node tools/rotate-vip-codes.js`). «Случайный» VIP снимается при входе (`auditVip`).
- **Промокоды:** теперь это не читаемые слова, а случайные наборы букв и цифр блоками по четыре (`XXXX-XXXX-XXXX`) — их нельзя угадать по смыслу. Актуальные коды и их награды — в **`codes/promo-codes.md`** (файл в репозитории). Регистр и пробелы при вводе не важны.
- **VIP-коды** (`VIP-XXXX-XXXX`, продаются за 150 ₽ через почту) — в приватном файле **`vip_codes_funpay.txt`** (в `.gitignore` и в деплое его нет); в клиенте лежат только их SHA-256-хеши.
- **Код автора `LEGENDA_PH`** (Легенда_пх, 1 канал Школа Дроп) привязан к игроку с уникальным ID **#878557**; тестовые коды удалены. Реестр теперь понимает ID вместо uid — сервер сам находит аккаунт.
- **Владельца забанить нельзя.** Аккаунт со статусом 👑 ВЛАДЕЛЕЦ (его выдаёт только владелец из максимальной панели) не банится и не удаляется ни администрацией, ни даже владельческим секретом; ютуберов/легенд/админов банит только владелец. Кнопка бана у таких игроков скрыта.

### 7. 😴 Кнопка «Включить сервер»
- Бесплатный Render засыпает. Если сервер не отвечает, в окнах сообщества/трейдинга и в шлагбауме появляется жёлтая плашка: «Сервер не работает — напишите нам на почту **shkoladrop.contact@gmail.com**, решим проблему, — или нажмите кнопку включить сервер».
- Кнопка сама будит сервер: пингует до ~2 минут, показывает обратный отсчёт и здоровается, когда сервер проснулся.

---

## 🤝 Онлайн-функции (сервер v1.2.0)

- **Сервер зашит в клиент** (`COMMUNITY_SERVER_URL`), ничего настраивать не нужно. Свой — одной командой: `node server/index.js` (Node 18+, **ноль npm-зависимостей**) → http://localhost:3377 (раздаёт и игру, и API).
- **🔐 Аккаунты:** `POST /api/account/start` (e-mail+пароль → режим вход/регистрация + демо-код), `POST /api/account/verify` (код → uid/ник/ID), `GET /api/account/exists`.
- **💬 Сообщество:** общий чат школы (дельта-опрос, модерация админом), поиск игрока по ID/нику, подарок найденному в один клик.
- **🆔 Уникальные ID #123456** выдаются автоматически при первой синхронизации и хранятся навсегда; **✔ галочки** выдает только админ из админ-панели и видны в профиле, чате и поиске.
- **☁️ Облачные сейвы:** push каждые ~45 секунд + на уходе со страницы (sendBeacon), автовосстановление на любом устройстве.
- **💜 Коды авторов (спонсорство):** 10% стоимости кейсов копится владельцу кода (с игрока ничего не списывается); реестр — `author-codes.json` в корне репо.
- **🎁 Подарки и 🤝 трейдинг:** подарок по ID/нику, комнаты обмена по 6-значному коду с атомарной выдачей обеим сторонам.
- Подробности запуска и деплоя: **[docs/SERVER_START.md](docs/SERVER_START.md)**.

---

## 🛠 Техническое устройство

```
index.html        — весь UI игры (включая модалки: аккаунт/сообщество/трейдинг/админка)
js/config.js      — данные: кейсы, предметы, экономика. СЕЗОН 4.1: никаких кодов админки
                    и серверных секретов — только SHA-256-хеши VIP-кодов
js/storage.js     — сэйв v14, cookie-бэкап, вайп-миграции (переносит только ники/ID)
js/game.js        — ядро игры (кейсы, апгрейдер, лавка, инвентарь, миссии...) + навигация
js/crash.js       — мини-игра «Ракета» (Crash): полёт, ставки, автовывод, канвас
                    и SVG-спрайт ракеты (#crashRocket), статистика
js/icons.js       — встроенные SVG-иконки 24×24 (в т.ч. заливной спрайт rocketSolid)
                    вместо системных эмодзи
js/games.js       — единое меню мини-игр (карточки из реестра MINI_GAMES;
                    экран «Режимы» — тоже карточка этого меню)
js/netplay.js     — онлайн: AuthGate/Account, сообщество, чат, ID, облако, wake-сервер,
                    AdminAuth (вход в панель по серверному токену)
js/ui.js, preloader.js, main.js, craft.js, modes.js, bottle.js
server/index.js   — сервер сообщества (Node 18+, ноль зависимостей, JSON-база):
                    белый список статики, вход в админку по токену, внешний сейф данных
server/vault.js   — «сейф» базы: файл, приватный GitHub-репозиторий или
                    HTTP-хранилище — данные переживают сон и редеплой Render
server/selftest.js— 136 автопроверок сервера (API, аккаунты, сезоны)
tools/rotate-vip-codes.js — выпуск новых VIP-кодов (старые аннулируются)
author-codes.json — реестр кодов авторов (наружу не отдаётся)
```

### Тесты
- `node server/selftest.js` — **136/136**: все API, аккаунты (регистрация/вход/коды/ошибки), одноразовый вайп с рестартами.
- `npm run test:unit` — **56/56**: математика «Ракеты» (распределение краша, пол 1,20×, шансы долететь до 1,5–10×, RTP, рост множителя, выплаты) и реестр мини-игр.
- `npm run test:ui` — **91/91** (jsdom): загрузка страницы, меню «Игры», переключение игр, полный раунд ракеты (автовывод, взрыв, защита от двойного списания, валидация ставок), **спрайт ракеты виден и летит по траектории**, 10 раундов подряд с проверкой коэффициентов.
- `npm run test:lint` — **34/34**: целостность разметки и кода (нет битых `id`, нет обработчиков без функций, реестр игр согласован с HTML, спрайт ракеты есть в HTML/CSS/JS, адаптивность и иконки).
- `npm run test:security` — **51/51**: статика не отдаёт `codes/`, `author-codes.json`, `tests/`, `server/`; старые код `1337` и секрет `david-admin-1337` не пускают; вход по коду выдаёт токен; подделка/просрочка токена и антибрутфорс; в клиенте нет кодов/секретов; утёкшие VIP-коды аннулированы, новые работают.
- `npm run test:vault` — **21/21**: база уезжает во внешний сейф (файл, HTTP и GitHub), «Render стирает диск» → сервер восстанавливает прогресс, чат и карточку игрока из сейфа; выключенный сейф ничего не ломает.
- `npm run test:all` — всё разом (**477 проверок**).
- **Документация:** [`docs/БЕЗОПАСНОСТЬ_И_ДАННЫЕ.md`](docs/БЕЗОПАСНОСТЬ_И_ДАННЫЕ.md) — что утекало, что сделано, что нажать в Render/Vercel.

---

## 📜 Что было в прошлых сезонах (коротко)

- **3.5/3.6** — прелоадер, Лаборатория 3.6 (бета-тест по секретному коду, ник «Тест», неоновый бейдж, бета-кейсы).
- **Сообщество v1.1** — чат, уникальные ID, галочки верификации, облачные сейвы; убран ручной JSON-бэкап.
- **Сезон 3** — кейсы ютуберов (вплоть до Бриллиантовой кнопки), бесконечный рост экономики.
- **Сезон 2** — 15 кейсов, коты-предметы, достижения/уровни, пассивный доход, секретный Кот-Хранитель.

Полный архив планов: `docs/`. Идеи и баги — на **shkoladrop.contact@gmail.com** 📮

*ШКОЛА ДРОП — это фан-симулятор. Никаких реальных денег: котлеты из буфета тоже ненастоящие.* 🥟
